class Stock {
  final String description;
  final String symbol;
  final String currency;
  final String type;
  final String logo;

  Stock(this.description, this.symbol, this.currency, this.type, this.logo);
}
